---
import { Image } from 'astro:assets';
import Layout from '../layouts/Layout.astro';

import Timeline from '../components/sections/nosotros/timeline.astro'

import Mvv from '../components/sections/nosotros/mv.astro'

import nosotrosHero from '../assets/images/nosotros/nosotrosHero.jpg';

import ValoresZigZag from '../components/sections/nosotros/valoresZigZag.astro';
import {valores} from '../utils/valores'
import { gerentes } from '../utils/gerente';
import { heroImages } from '../data/homepage';

import NosotrosSplit from '../components/sections/nosotros/nosotrosSplit.astro';
import ProjectCta from '../components/sections/shared/ProjectCta.astro';
---

<Layout title="Valcer - Nosotros" heroTitle="Nosotros" heroImage={nosotrosHero}>
  <section class="section-2 gerente-section">
    <div class="container gerente-slider" data-gerente-slider>
      {gerentes.map((gerente, index) => (
        <div
          class={`gerente-grid${index === 0 ? " is-active" : ""}`}
          data-gerente-slide
          aria-hidden={index === 0 ? "false" : "true"}
        >
          <div class="gerente-photo">
            <Image
              src={gerente.photo}
              alt={gerente.alt}
              width={720}
              height={900}
              quality={85}
              densities={[1, 2]}
            />
          </div>

          <div class="gerente-copy">
            <blockquote class="gerente-quote">
              "{gerente.message}"
            </blockquote>
            <p class="gerente-name">{gerente.name}</p>
            <p class="gerente-role">{gerente.role}</p>
          </div>
        </div>
      ))}

      <div class="gerente-dots" aria-label="Cambiar mensaje de gerencia">
        {gerentes.map((gerente, index) => (
          <button
            type="button"
            class={`gerente-dot${index === 0 ? " is-active" : ""}`}
            data-gerente-dot
            data-index={index}
            aria-label={`Ver mensaje de ${gerente.name}`}
            aria-pressed={index === 0 ? "true" : "false"}
          ></button>
        ))}
      </div>
    </div>
  </section>

  <!-- Quienes somos -->
  <NosotrosSplit
    images={heroImages}
  />

  <!-- Pilares empresa -->
  <ValoresZigZag items={valores} />

  <!-- Mision vision y valores -->
  <Mvv
    leftTag="MISION"
    rightTag="VISION"
    mission={{
      title: "Cómo trabajamos",
      body: "Brindar servicios de ejecución, supervisión y consultoría en obras civiles y de esta manera podamos satisfacer las necesidades de nuestros clientes entregando un servicio de calidad bajo las normas y leyes vigentes, propiciando un ambiente de trabajo seguro.",
    }}
    vision={{
      title: "Hacia dónde vamos",
      body: "Ser una empresa líder, y promotor en el desarrollo del país a nivel nacional ejecutando los mejores proyectos constructivos, aplicando los más altos estándares de calidad.",
    }}
  />

  <Timeline />

  <ProjectCta />
</Layout>

<style>
  .gerente-section {
    padding-top: clamp(1rem, 3vw, 2rem);
    overflow-x: clip;
  }

  .gerente-slider {
    max-width: 1040px;
    margin: 0 auto;
    position: relative;
    min-height: 540px;
  }

  .gerente-grid {
    display: grid;
    grid-template-columns: minmax(220px, 0.62fr) minmax(0, 1.38fr);
    gap: clamp(1.4rem, 4vw, 3rem);
    align-items: center;
    justify-content: center;
    position: absolute;
    inset: 0;
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
    transition: opacity 0.55s ease, visibility 0.55s ease;
  }

  .gerente-grid.is-active {
    opacity: 1;
    visibility: visible;
    pointer-events: auto;
  }
  .gerente-photo {
    max-width: 340px;
    width: 100%;
    border-radius: 5px;
    overflow: hidden;
    border: 1px solid rgba(42, 43, 111, 0.08);
    box-shadow: 0 10px 22px rgba(32, 39, 87, 0.06);
    background: rgba(255, 255, 255, 0.96);
  }

  .gerente-photo :global(img) {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
    aspect-ratio: 4 / 5;
  }

  .gerente-copy {
    padding: clamp(1rem, 2vw, 1.4rem) 0;
    max-width: 42rem;
  }
  .gerente-quote {
    position: relative;
    margin: 0;
    padding-left: 1.8rem;

    color: var(--color-white);
    font-size: clamp(1.15rem, 2vw, 1.5rem);
    line-height: 1.65;

    font-weight: 650;
    text-shadow: 0 2px 10px rgba(0, 0, 0, 0.25);
    max-width: 38ch;

    text-align: justify; /* 👈 AQUÍ */
  }

  .gerente-quote::before,
  .gerente-quote::after {
    position: absolute;
    color: rgba(42, 43, 111, 0.2);
    font-family: Garamond, Baskerville, "Palatino Linotype", "Book Antiqua", serif;
    font-size: clamp(3rem, 5vw, 4.5rem);
    line-height: 1;
  }

  .gerente-quote::before {
    content: '“';
    top: -0.25rem;
    left: 0;
  }

  .gerente-quote::after {
    content: '”';
    right: 0;
    bottom: -1.1rem;
  }

  .gerente-name {
    margin: 1.25rem 0 0;
    color: var(--color-white);
    font-size: var(--text-sm);
    font-weight: 700;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .gerente-role {
    margin: 0.35rem 0 0;
    color: var(--color-white); 
    font-size: var(--text-sm);
    font-weight: 700; 
    text-align: left;

  }
  .gerente-dots {
    display: flex;
    justify-content: center;
    gap: 0.55rem;
    margin-top: 1.25rem;
    position: absolute;
    left: 50%;
    bottom: 0;
    transform: translateX(-50%);
    width: max-content;
  }

  .gerente-dot {
    width: 10px;
    height: 10px;
    padding: 0;
    border: 0;
    border-radius: 999px;
    background: rgba(42, 43, 111, 0.16);
    cursor: pointer;
    transition: transform 0.2s ease, background 0.2s ease;
  }

  .gerente-dot.is-active {
    background: var(--color-primary);
    transform: scale(1.15);
  }

  .pillars-grid{
    --overlap: 18px;
    --cut: 44px;

    display: grid;
    gap: 0;
    margin-top: 18px;

    max-width: 980px;
    margin-left: auto;
    margin-right: auto;
  }

  @media (max-width: 900px) {
    .gerente-slider {
      min-height: 820px;
    }

    .gerente-grid {
      grid-template-columns: 1fr;
      gap: 1.1rem;
    }

    .gerente-photo {
      max-width: 300px;
      width: 100%;
      margin: 0 auto;
    }

    .gerente-copy {
      padding-top: 0;
      max-width: none;
    }

    .gerente-name,
    .gerente-role {
      text-align: left;
      width: 100%;
    }
    .gerente-quote {
      max-width: none;
      padding-left: 0.9rem;
      padding-right: 0.9rem;
    }
  }

  @media (max-width: 640px) {
    .gerente-slider {
      min-height: 900px;
      padding-bottom: 2.5rem;
    }

    .gerente-grid {
      inset: 0 0 2.5rem;
      gap: 0.9rem;
    }

    .gerente-photo {
      max-width: min(100%, 260px);
    }

    .gerente-copy {
      padding-inline: 0.25rem;
    }

    .gerente-kicker {
      font-size: 0.68rem;
      letter-spacing: 0.06em;
      margin-bottom: 0.85rem;
    }

    .gerente-quote {
      padding-inline: 0.35rem;
      font-size: clamp(1rem, 4.6vw, 1.18rem);
      line-height: 1.55;
    }

    .gerente-quote::before,
    .gerente-quote::after {
      font-size: clamp(2.25rem, 11vw, 3.2rem);
    }

    .gerente-quote::after {
      bottom: -0.7rem;
    }

    .gerente-name {
      margin-top: 1rem;
    }

    .gerente-dots {
      bottom: 0.2rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .gerente-grid {
      transition: none;
    }

    .gerente-dot {
      transition: none;
    }
  }
</style>

<script>
  function initNosotrosPage() {
    const slider = document.querySelector<HTMLElement>('[data-gerente-slider]');
    const slides = Array.from(document.querySelectorAll<HTMLElement>('[data-gerente-slide]'));
    const dots = Array.from(document.querySelectorAll<HTMLButtonElement>('[data-gerente-dot]'));
    let activeIndex = 0;
    let timerId: number | undefined;

    function showSlide(index: number) {
      slides.forEach((slide, slideIndex) => {
        const isActive = slideIndex === index;
        slide.classList.toggle('is-active', isActive);
        slide.setAttribute('aria-hidden', isActive ? 'false' : 'true');
      });

      dots.forEach((dot, dotIndex) => {
        const isActive = dotIndex === index;
        dot.classList.toggle('is-active', isActive);
        dot.setAttribute('aria-pressed', isActive ? 'true' : 'false');
      });

      activeIndex = index;
    }

    function startSlider() {
      if (slides.length <= 1) return;
      window.clearInterval(timerId);
      timerId = window.setInterval(() => {
        showSlide((activeIndex + 1) % slides.length);
      }, 5000);
    }

    if (slider && slides.length) {
      dots.forEach((dot, index) => {
        dot.addEventListener('click', () => {
          showSlide(index);
          startSlider();
        });
      });

      slider.addEventListener('mouseenter', () => window.clearInterval(timerId));
      slider.addEventListener('mouseleave', startSlider);

      showSlide(0);
      startSlider();
    }

    const pillars = document.querySelectorAll<HTMLElement>('.pillar');
    if (!pillars.length) return;

    const observer = new IntersectionObserver((entries, obs) => {
      entries.forEach((entry) => {
        if (!entry.isIntersecting) return;
        const el = entry.target as HTMLElement;
        const index = [...pillars].indexOf(el);
        el.style.transitionDelay = `${index * 120}ms`;
        el.classList.add('is-visible');
        obs.unobserve(el);
      });
    }, { threshold: 0.15, rootMargin: "0px 0px -10% 0px" });

    pillars.forEach((p) => observer.observe(p));
  }

  initNosotrosPage();
</script>
