---
const currentPath = Astro.url.pathname;
const isActive = (path: string) =>
  path === "/" ? currentPath === "/" : currentPath === path || currentPath.startsWith(`${path}/`);
---

<header class="header">
  <nav class="nav" aria-label="Navegacion principal">
    <div class="logo">
      <a href="/">
        <img src="/icons/logo/logo_completo.svg" alt="Valcer" class="logo-img" />
      </a>
    </div>

    <button
      class="menu-toggle"
      data-menu-toggle
      aria-label="Abrir menu"
      aria-expanded="false"
      aria-controls="navLinks"
    >
      <span></span>
      <span></span>
      <span></span>
    </button>

    <ul class="nav-links" data-nav-links id="navLinks">
      <li>
        <a href="/" class={isActive('/') ? 'active' : ''}>Inicio</a>
      </li>
      <li>
        <a href="/nosotros" class={isActive('/nosotros') ? 'active' : ''}>Nosotros</a>
      </li>
      <li>
        <a href="/proyectos" class={isActive('/proyectos') ? 'active' : ''}>Proyectos</a>
      </li>
      <li>
        <a href="/servicios" class={isActive('/servicios') ? 'active' : ''}>Servicios</a>
      </li>
      <li>
        <a href="/contacto" class={isActive('/contacto') ? 'active' : ''}>Contacto</a>
      </li>
    </ul>
  </nav>
  <button class="nav-backdrop" data-nav-backdrop aria-hidden="true" tabindex="-1"></button>
</header>

<style>
  .logo a {
    display: flex;
    align-items: center;
  }

  .logo-img {
    height: 42px;
    width: auto;
    display: block;
    object-fit: contain;
    transition: filter var(--transition-fast);
    
    /* convierte negro → blanco */
    filter: brightness(0) invert(1);
  }

  .header.scrolled .logo-img {
    filter: none;
  }
  .header {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: var(--header-height);
    background: transparent;
    z-index: 1000;
    transition: background 0.3s ease;
  }

  .header.scrolled {
    background: rgba(255, 255, 255, 0.9);
    backdrop-filter: blur(14px);
    border-bottom: 1px solid rgba(42, 43, 111, 0.08);
    box-shadow: var(--shadow-sm);
  }

  .header.scrolled .logo a,
  .header.scrolled .nav-links a {
    color: var(--color-text);
  }

  .header.scrolled .nav-links a.active {
    color: var(--color-primary);
    text-decoration: none;
  }

  .header.scrolled .menu-toggle span {
    background: var(--color-text);
  }

  .nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    max-width: var(--container-width);
    height: 100%;
    margin: 0 auto;
    padding: 0 var(--spacing-sm);
  }

  .logo a {
    font-size: var(--text-xl);
    font-weight: 700;
    color: white;
    transition: color var(--transition-fast);
  }

  .logo a:hover {
    color: var(--color-primary);
  }

  .logo a:focus-visible,
  .nav-links a:focus-visible {
    outline: 2px solid var(--color-primary);
    outline-offset: 4px;
    border-radius: 2px;
  }

  .menu-toggle {
    display: none;
    flex-direction: column;
    gap: 5px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 8px;
  }

  .menu-toggle:focus-visible {
    outline: 2px solid var(--color-primary);
    outline-offset: 2px;
    border-radius: 4px;
  }

  .menu-toggle span {
    width: 25px;
    height: 3px;
    background: white;
    transition: all var(--transition-normal);
    border-radius: 2px;
  }

  .menu-toggle.active span:nth-child(1) {
    transform: rotate(45deg) translate(5px, 5px);
  }

  .menu-toggle.active span:nth-child(2) {
    opacity: 0;
  }

  .menu-toggle.active span:nth-child(3) {
    transform: rotate(-45deg) translate(7px, -6px);
  }

  .nav-links {
    display: flex;
    align-items: center;
    list-style: none;
    gap: var(--spacing-md);
    margin: 0;
    padding: 0;
  }

  .nav-links li {
    display: flex;
    align-items: center;
  }

  .nav-links a {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    position: relative;
    color: white;
    font-weight: 500;
    font-size: var(--text-base);
    line-height: 1.1;
    min-height: 2.5rem;
    padding: 0.2rem 0;
    transition: color var(--transition-fast);
  }

  .nav-links a:hover {
    color: var(--color-secondary);
  }

  .nav-links a.active {
    color: white;
    font-weight: 600;
    text-decoration: none;
  }

  .nav-links a::after {
    content: "";
    position: absolute;
    left: 0;
    right: 0;
    bottom: 0.15rem;
    height: 2px;
    border-radius: 999px;
    background: transparent;
    transition: background var(--transition-fast);
  }

  .nav-links a.active::after {
    background: rgba(109, 186, 235, 0.9);
  }

  .nav-backdrop {
    display: none;
  }

  @media (max-width: 768px) {

    .logo-img {
      height: 36px;
    }

    .menu-toggle {
      display: flex;
      z-index: 1001;
    }

    .nav-links {
      position: fixed;
      top: var(--header-height);
      left: 0;
      width: 100%;
      height: calc(100vh - var(--header-height));
      background:
        linear-gradient(180deg, rgba(109, 186, 235, 0.12), rgba(255, 255, 255, 0.96)),
        var(--color-background);
      flex-direction: column;
      gap: 0;
      padding: var(--spacing-md) 0;
      transform: translateX(-100%);
      transition: transform var(--transition-normal);
      box-shadow: var(--shadow-md);
      z-index: 1000;
    }

    .nav-links.active {
      transform: translateX(0);
    }

    .nav-backdrop {
      display: block;
      position: fixed;
      inset: var(--header-height) 0 0 0;
      border: 0;
      background: rgba(12, 16, 40, 0.42);
      opacity: 0;
      pointer-events: none;
      transition: opacity var(--transition-normal);
      z-index: 999;
    }

    .nav-backdrop.active {
      opacity: 1;
      pointer-events: auto;
    }

    .nav-links li {
      display: block;
      border-bottom: 1px solid var(--color-border);
    }

    .nav-links a {
      display: block;
      padding: var(--spacing-sm) var(--spacing-md);
      font-size: var(--text-lg);
      color: var(--color-text);
      min-height: auto;
    }

    .nav-links a.active {
      color: var(--color-primary);
    }

    .nav-links a::after {
      left: var(--spacing-md);
      right: var(--spacing-md);
      bottom: 0.55rem;
    }

    .nav-links a.active::after {
      background: rgba(42, 43, 111, 0.35);
    }
  }
</style>

<script>
  const headerStore = (window.__valcerHeader ??= { cleanup: null });

  function initHeader() {
    headerStore.cleanup?.();

    const header = document.querySelector<HTMLElement>('.header');
    const menuToggle = document.querySelector<HTMLButtonElement>('[data-menu-toggle]');
    const navLinks = document.querySelector<HTMLUListElement>('[data-nav-links]');
    const backdrop = document.querySelector<HTMLButtonElement>('[data-nav-backdrop]');
    if (!header || !menuToggle || !navLinks || !backdrop) return;

    const signalController = new AbortController();
    const { signal } = signalController;
    const isMobileMq = window.matchMedia('(max-width: 768px)');
    const links = Array.from(navLinks.querySelectorAll('a'));

    const lockScroll = () => {
      document.documentElement.classList.add('modal-open');
      document.body.classList.add('modal-open');
    };

    const unlockScroll = () => {
      document.documentElement.classList.remove('modal-open');
      document.body.classList.remove('modal-open');
    };

    const applyMenuState = (open: boolean) => {
      menuToggle.classList.toggle('active', open);
      navLinks.classList.toggle('active', open);
      backdrop.classList.toggle('active', open);
      menuToggle.setAttribute('aria-expanded', String(open));
      menuToggle.setAttribute('aria-label', open ? 'Cerrar menu' : 'Abrir menu');

      if (isMobileMq.matches) {
        navLinks.toggleAttribute('inert', !open);
        navLinks.setAttribute('aria-hidden', String(!open));
      } else {
        navLinks.removeAttribute('inert');
        navLinks.setAttribute('aria-hidden', 'false');
      }

      if (open && isMobileMq.matches) {
        lockScroll();
        links[0]?.focus();
      } else {
        unlockScroll();
      }
    };

    const closeMenu = () => applyMenuState(false);
    const handleToggle = () => applyMenuState(!navLinks.classList.contains('active'));
    const handleScroll = () => {
      header.classList.toggle('scrolled', window.scrollY > 50);
    };

    menuToggle.addEventListener('click', handleToggle, { signal });
    backdrop.addEventListener('click', closeMenu, { signal });

    links.forEach((link) => {
      link.addEventListener('click', closeMenu, { signal });
    });

    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && navLinks.classList.contains('active')) {
        closeMenu();
        menuToggle.focus();
      }
    }, { signal });

    window.addEventListener('resize', () => {
      if (!isMobileMq.matches) closeMenu();
      applyMenuState(navLinks.classList.contains('active'));
    }, { signal, passive: true });

    window.addEventListener('scroll', handleScroll, { signal, passive: true });
    handleScroll();
    applyMenuState(false);

    headerStore.cleanup = () => {
      signalController.abort();
      closeMenu();
      headerStore.cleanup = null;
    };
  }

  initHeader();
  document.addEventListener('astro:page-load', initHeader);
  document.addEventListener('astro:before-preparation', () => {
    headerStore.cleanup?.();
  });
</script>
